{
  "name": "pomerium-downstream-mtls-e2e",
  "version": "1.0.0",
  "description": "E2E tests for Pomerium downstream mTLS, run inside a Playwright container. The @playwright/test pin must match the PLAYWRIGHT_IMAGE tag (see Makefile/README).",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "@types/node": "^26.0.1",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
