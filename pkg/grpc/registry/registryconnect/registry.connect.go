// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: registry.proto

package registryconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	registry "github.com/pomerium/pomerium/pkg/grpc/registry"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// RegistryName is the fully-qualified name of the Registry service.
	RegistryName = "registry.Registry"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// RegistryReportProcedure is the fully-qualified name of the Registry's Report RPC.
	RegistryReportProcedure = "/registry.Registry/Report"
	// RegistryListProcedure is the fully-qualified name of the Registry's List RPC.
	RegistryListProcedure = "/registry.Registry/List"
	// RegistryWatchProcedure is the fully-qualified name of the Registry's Watch RPC.
	RegistryWatchProcedure = "/registry.Registry/Watch"
)

// RegistryClient is a client for the registry.Registry service.
type RegistryClient interface {
	// Report is periodically sent by each service to confirm it is still serving
	// with the registry data is persisted with a certain TTL
	Report(context.Context, *connect.Request[registry.RegisterRequest]) (*connect.Response[registry.RegisterResponse], error)
	// List returns current snapshot of the services known to the registry
	List(context.Context, *connect.Request[registry.ListRequest]) (*connect.Response[registry.ServiceList], error)
	// Watch returns a stream of updates
	// for the simplicity of consumer its delivered as full snapshots
	// and is only sent when change is detected
	Watch(context.Context, *connect.Request[registry.ListRequest]) (*connect.ServerStreamForClient[registry.ServiceList], error)
}

// NewRegistryClient constructs a client for the registry.Registry service. By default, it uses the
// Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewRegistryClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) RegistryClient {
	baseURL = strings.TrimRight(baseURL, "/")
	registryMethods := registry.File_registry_proto.Services().ByName("Registry").Methods()
	return &registryClient{
		report: connect.NewClient[registry.RegisterRequest, registry.RegisterResponse](
			httpClient,
			baseURL+RegistryReportProcedure,
			connect.WithSchema(registryMethods.ByName("Report")),
			connect.WithClientOptions(opts...),
		),
		list: connect.NewClient[registry.ListRequest, registry.ServiceList](
			httpClient,
			baseURL+RegistryListProcedure,
			connect.WithSchema(registryMethods.ByName("List")),
			connect.WithClientOptions(opts...),
		),
		watch: connect.NewClient[registry.ListRequest, registry.ServiceList](
			httpClient,
			baseURL+RegistryWatchProcedure,
			connect.WithSchema(registryMethods.ByName("Watch")),
			connect.WithClientOptions(opts...),
		),
	}
}

// registryClient implements RegistryClient.
type registryClient struct {
	report *connect.Client[registry.RegisterRequest, registry.RegisterResponse]
	list   *connect.Client[registry.ListRequest, registry.ServiceList]
	watch  *connect.Client[registry.ListRequest, registry.ServiceList]
}

// Report calls registry.Registry.Report.
func (c *registryClient) Report(ctx context.Context, req *connect.Request[registry.RegisterRequest]) (*connect.Response[registry.RegisterResponse], error) {
	return c.report.CallUnary(ctx, req)
}

// List calls registry.Registry.List.
func (c *registryClient) List(ctx context.Context, req *connect.Request[registry.ListRequest]) (*connect.Response[registry.ServiceList], error) {
	return c.list.CallUnary(ctx, req)
}

// Watch calls registry.Registry.Watch.
func (c *registryClient) Watch(ctx context.Context, req *connect.Request[registry.ListRequest]) (*connect.ServerStreamForClient[registry.ServiceList], error) {
	return c.watch.CallServerStream(ctx, req)
}

// RegistryHandler is an implementation of the registry.Registry service.
type RegistryHandler interface {
	// Report is periodically sent by each service to confirm it is still serving
	// with the registry data is persisted with a certain TTL
	Report(context.Context, *connect.Request[registry.RegisterRequest]) (*connect.Response[registry.RegisterResponse], error)
	// List returns current snapshot of the services known to the registry
	List(context.Context, *connect.Request[registry.ListRequest]) (*connect.Response[registry.ServiceList], error)
	// Watch returns a stream of updates
	// for the simplicity of consumer its delivered as full snapshots
	// and is only sent when change is detected
	Watch(context.Context, *connect.Request[registry.ListRequest], *connect.ServerStream[registry.ServiceList]) error
}

// NewRegistryHandler builds an HTTP handler from the service implementation. It returns the path on
// which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewRegistryHandler(svc RegistryHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	registryMethods := registry.File_registry_proto.Services().ByName("Registry").Methods()
	registryReportHandler := connect.NewUnaryHandler(
		RegistryReportProcedure,
		svc.Report,
		connect.WithSchema(registryMethods.ByName("Report")),
		connect.WithHandlerOptions(opts...),
	)
	registryListHandler := connect.NewUnaryHandler(
		RegistryListProcedure,
		svc.List,
		connect.WithSchema(registryMethods.ByName("List")),
		connect.WithHandlerOptions(opts...),
	)
	registryWatchHandler := connect.NewServerStreamHandler(
		RegistryWatchProcedure,
		svc.Watch,
		connect.WithSchema(registryMethods.ByName("Watch")),
		connect.WithHandlerOptions(opts...),
	)
	return "/registry.Registry/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case RegistryReportProcedure:
			registryReportHandler.ServeHTTP(w, r)
		case RegistryListProcedure:
			registryListHandler.ServeHTTP(w, r)
		case RegistryWatchProcedure:
			registryWatchHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedRegistryHandler returns CodeUnimplemented from all methods.
type UnimplementedRegistryHandler struct{}

func (UnimplementedRegistryHandler) Report(context.Context, *connect.Request[registry.RegisterRequest]) (*connect.Response[registry.RegisterResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("registry.Registry.Report is not implemented"))
}

func (UnimplementedRegistryHandler) List(context.Context, *connect.Request[registry.ListRequest]) (*connect.Response[registry.ServiceList], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("registry.Registry.List is not implemented"))
}

func (UnimplementedRegistryHandler) Watch(context.Context, *connect.Request[registry.ListRequest], *connect.ServerStream[registry.ServiceList]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("registry.Registry.Watch is not implemented"))
}
